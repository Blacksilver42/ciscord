// Ciscord example bot

#include "ciscord.h"

int main(int argc, char * argv[]){
	CDIS D = cdis_init(NULL);
	find_key(argc, argv);
	
	//guild_t * gld = Dfind_guild_id(cdis, NULL, "391017604971626509");
	//chan_t * general = Dfind_chan_id( cdis, NULL, "391017605420285963");
	//ctx_t * ctx = makectx(NULL, gld, general);
	
	//Dsend(cdis, NULL, "Hello, world!");

	
	
	
	
	return 0;
}
